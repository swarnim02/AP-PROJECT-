services:
  - type: web
    name: hostel-backend
    env: node
    buildCommand: npm install && npx prisma generate && npx prisma migrate deploy && npm run setup-db
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5002